{
  "name": "clear-credit-score",
  "version": "1.0.0",
  "description": "Clear Credit Score application with comprehensive logging",
  "type": "module",
  "scripts": {
    "build": "npm run build:middleware && npm run build:backend && npm run build:frontend",
    "build:middleware": "cd middleware && npm run build",
    "build:backend": "echo 'Backend build'",
    "build:frontend": "echo 'Frontend build'",
    "dev": "npm run dev:backend & npm run dev:frontend",
    "dev:backend": "cd backend && node src/api/shortenerApi.js",
    "dev:frontend": "cd frontend && npm start"
  },
  "workspaces": [
    "middleware",
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}